# 词加加 问题日志 / 修改记录

> 用途：记录词加加从开发到上线遇到的真实现象、原因和修复，作为面试与复盘的第一手资料。
> 原则：只写真实发生过的事；没确认原因就标“待排查”，不编造“我修过某某问题”。

## 记录规范

每条记录尽量包含：

- 日期
- 类别：部署 / 后端 / 数据库 / 前端交互 / 手机适配 / 数据内容 / 需求调整
- 现象：用户或测试看到什么
- 原因：为什么发生
- 处理：怎么修或怎么决定
- 状态：已修复 / 已收口 / 待排查 / 待复现

---

## 2026-09-07 ~ 09-08

### busy_timeout + 基础日志上线（后端技术点）

- 类别：后端 / 数据库
- 现象：词加加已有少量真实用户，需要更稳的数据库写入等待，并留下可排查的线上请求记录。
- 处理：`sqlite3.connect(timeout=10)` 并执行 `PRAGMA busy_timeout = 10000`；新增 `logs/wordapp.log` 轮转日志（单文件 1MB、保留 3 份），每次请求记录方法、路径、状态码与耗时；API 未处理异常统一返回 JSON 500 并写日志。
- 验证：本地注册/打卡跑通，PythonAnywhere 上传新 `deploy-logging-20260908.zip` 并 Reload 后，手机访问线上，`logs/wordapp.log` 出现真实请求记录。
- 状态：已上线。



### 多词书与独立进度上线（功能收口）

- 类别：需求调整 / 前端交互
- 现象：原先是单词库打卡，用户希望像背单词 App 一样可以切换词书；每次切换不能互相覆盖进度。
- 处理：改为独立词书页，正式词书为「山西专升本500高频词 / 1800词 / 全国专升本500高频词」，删除「高频词补充」；每本书独立进度、每日新词数量可自定义，保留艾宾浩斯复习。
- 状态：已上线，GitHub main 与 PythonAnywhere 已同步。

### 线上更新后手机上没看到最新版（部署）

- 类别：部署
- 现象：本地测试是新版，但手机打开线上仍不是最新效果。
- 原因：新版前端文件没有全部传到 PythonAnywhere 的 `wordapp` 对应目录，或上传后没有 Reload。
- 处理：把 `app.py`、`index.html`、`static/hf500-data.js` 传到 `/home/qiwenci/wordapp` 对应位置并 Reload；`official.db` 保留不覆盖。
- 状态：已修复。

### 登录提示“手机号或密码不对”（待排查）

- 类别：后端 / 账号
- 现象：用户用自己的手机号和密码登录线上，页面提示“手机号或密码不对”；用户认为手机号和密码没有填错。
- 状态：待复现后按真实日志排查。检查方向：注册账号是否存在于线上 `official.db`、`admin_phone.txt` 指定的手机号是否已注册、密码是否满足管理员强密码要求、本地库和线上库是否同一份。
- 备注：这个事件没有确认真实根因，不能当成面试故事讲，先作为待排查记录保存。

### 手机端和电脑端交互差异（观察记录）

- 类别：前端交互 / 手机适配
- 现象：电脑浏览器里“现在是第几天”“修改起始日期”等部分控件点不开；用户反馈手机上可以正常操作。
- 状态：用户判断不影响当前使用，暂不处理；以后做真机回归时再查。

### 返回按钮视觉与位置迭代（手机适配）

- 类别：前端交互 / 手机适配
- 现象：返回按钮原先有白色圆形底，用户觉得丑且会和内容重叠；手机真机上还会被屏幕边缘/安全区挡住。
- 处理：去掉白色圆片，只保留清晰的返回箭头；按手机实机反复微调位置，最终靠近屏幕边缘并避开安全区。
- 状态：已收口。

### 词书页与每日数量修改按钮样式迭代

- 类别：前端交互
- 现象：词书名过长会折成两行，选择词书区域太窄；“修改每日数量”按钮样式先后试过白框、浅蓝底、细边框等多种方案，用户都觉得不够协调。
- 处理：加宽词书选择区域，较长名称的“500词当前”信息换行到第二行，名称本身尽量保持一行；每日数量按钮最终定为极浅蓝底、无边框、黑色文字；选中词书时按钮反色为白底。
- 状态：已收口。

### 词卡考频备注格式调整（数据内容）

- 类别：数据内容
- 现象：部分词来源表里某页某行找不到对应内容，属于制作资料本身的疏漏。
- 处理：无法按年份归类的词统一写“16-26年山西真题未识别”；备注统一为第一行“考频 X 次 · 覆盖 X 年”，第二行写具体年份，并居中显示。
- 状态：已收口。

---

## 早期记录（2026-07 ~ 08，日期以旧交接档为准）

### 旧版图片数据占用空间（数据库清理）

- 类别：数据库
- 现象：旧版本允许往单词里塞图片数据，导致用户 state 数据变大，占用 PythonAnywhere 免费磁盘额度。
- 处理：启动时清理旧 `wordImages` 字段，之后不再统计图片数据。
- 状态：已处理。

### 管理员账号规则调整（账号安全）

- 类别：后端 / 账号
- 现象：早期“第一个注册者自动是管理员”的规则不可控。
- 处理：改为部署时用 `admin_phone.txt` 指定管理员，每次启动同步管理员角色；新注册用户一律普通用户。
- 状态：已处理。

### 注册名额与容量限制（后端策略）

- 类别：后端 / 数据库
- 现象：免费额度有限，需要防止无限注册和自定义词库无限增长。
- 处理：增加注册名额上限设置、每人自定义词库总量上限 10000 词、管理员用量页显示数据库大小和容量百分比。
- 状态：已处理。

---

## 待记录 / 以后遇到再补

- 线上真实 500 错误：如果以后真的发生，先看 PythonAnywhere 日志和新增的应用日志，记下接口、时间、报错栈，再写修复。
- SQLite 锁冲突：如果以后真的出现 “database is locked”，再补当时的请求场景和修复方式。
- 用户行为数据：如果要记录“几点查词最多”，必须先明确隐私说明和用途，不能悄悄做。

## 2026-09-09 ~ 09-11

### 单词发音按钮上线（前端交互 / 手机适配）

- 现象：用户希望背词时能听发音，手机上原先的网页音效又容易受静音模式影响。
- 处理：词卡和记忆卡新增喇叭按钮，调用浏览器 `speechSynthesis`，优先使用 `en-US` 英文语音；用真实点击触发，不依赖服务器音频文件。
- 验证：电脑和手机均可正常发音。
- 状态：已上线。

### 连续打卡与累计完成计划天数（前端交互）

- 现象：第一天完成后，第二天尚未打卡时连续打卡显示 0；同一天连背两个计划日时，“第几天”和连续打卡看起来不一致。
- 原因：连续打卡原逻辑从今天开始回数，今天没有完成记录就直接停止；计划天数和自然日打卡本来就是两个不同指标。
- 处理：今天未完成时从昨天开始计算连续天数，保住已有连续记录；预留“累计完成计划天数”按钮/显示逻辑，按实际完成的计划日计数。
- 状态：已修复并上线。

### 先记新词再测试（需求调整 / 前端交互）

- 现象：同学反馈一进来就开始测试，没有先看一遍单词和释义的机会；复习词也不需要再单独记忆。
- 处理：每天先进入新词记忆阶段，显示单词、释义、发音和预留音标位置；新词记忆完自动进入测试，复习词直接进入测试；提供小“跳过记忆”按钮。
- 状态：已上线。

### 全国专升本500词权限隔离（后端 / 数据安全）

- 现象：全国500词来自朋友提供的资料，朋友老师要求不要外传，不能让所有登录用户看到或直接下载。
- 原因：原实现把 `HF500_WORDS` 和 `SX500_WORDS` 放在同一个公开静态文件中，只隐藏界面并不能真正保护数据。
- 处理：公开文件只保留山西500；全国500移到私有目录 `private_data/hf500_words.json`。后端新增 `/api/books/hf500`，只有 `hf500_phones.txt` 中列出的手机号通过登录校验后才能获取；其他账号看不到也拿不到词库。
- 状态：已部署，等待长期使用验证。

### app.py 缩进错误导致整站不可用（部署 / 后端）

- 现象：部署带权限逻辑的新 `app.py` 后，PythonAnywhere 显示 “Something went wrong :-)”，整站打不开。
- 原因：修改后端时 `return line` 的缩进少了一层，Python 启动时触发 `IndentationError: expected an indented block after 'if' statement on line 108`，Flask 应用无法导入。
- 处理：修正缩进，使用 `python -m py_compile app.py` 验证通过，再生成只包含 `app.py` 的恢复部署包并 Reload。
- 经验：后端变更打包前必须做 Python 编译检查；部署包尽量只带真实变化的文件；Reload 后先做首页、登录、核心 API 的最小健康检查。不能只检查前端 JavaScript 就直接上线。
- 状态：已修复。

## 2026-09-11 ~ 09-13

### 音频跨设备与 Quark 跳转问题（前端交互 / 手机适配）

- 现象：单词发音在开发者自己的手机和部分浏览器正常，但朋友手机、微信打开后转 Quark 等场景会没声音；有时出现双重发音。
- 原因：浏览器英语语音支持不一致；异步等待浏览器语音超时后，再播放 MP3 会失去点击授权；MP3 和手机语音的兜底竞态会导致重声。
- 处理：改为统一音频请求编号，任何时刻只允许一个单词音频；Quark 下按点击同步播放有道 MP3；其他浏览器优先浏览器语音，失败后走有道，最后使用本地 WAV。
- 经验：音频功能不能只测一台设备，必须覆盖首次加载、重复点击、微信内置浏览器、微信转外部浏览器、Quark、Chrome 和 Safari。
- 状态：已修复并持续验证。

### 答对答错音效延迟与缺失（前端交互）

- 现象：音效有时延迟，有时不播放，快速操作时更容易漏掉；音色后来与原始版本不一致。
- 原因：原实现依赖 Web Audio，部分手机不会解锁音频上下文；改成 WAV 后最初没有预加载，并复用同一个音频对象，快速播放会打断自身。
- 处理：按原 Web Audio 参数精确生成 48kHz WAV；页面打开时预取为 Blob；每次播放创建新的音频对象；不再共用会互相打断的播放器。
- 状态：已修复。

### 答错后重复点“显示答案”导致卡死（前端交互）

- 现象：自测中先显示答案，再点“答错了”，随后继续点“显示答案”，页面会停在答案界面，“继续”按钮消失，答对/答错也无响应。
- 原因：显示答案没有锁定，重复点击会用答案文本覆盖反馈区，把答错后动态追加的“继续”按钮抹掉；同时 `quiz.locked` 已经阻止后续评分。
- 处理：显示答案只能触发一次，显示后按钮禁用；题目锁定后不再修改反馈区；开始新题时重置显示状态；清理答对后延迟切题的旧定时器。
- 状态：已修复。

### 测试模式持久化（前端交互）

- 现象：用户切到选择题或手打答案后，下一题默认又回到自测。
- 处理：把最后选择的模式保存到用户状态；默认仍为自测，用户主动切换后，后续题目、重新进入和重新登录都沿用最后模式。
- 状态：已上线。

### 发布流程复盘（协作与质量）

- 问题：多种音频、交互竞态和手机适配问题先由用户真机发现，说明开发时只验证了功能是否能跑，没有系统覆盖真实操作路径。
- 改进：建立《发布前检查清单》，要求后端编译检查、前端语法检查、核心回归、音频专项、状态边界、部署回滚和真机测试；交付前必须说明已测、未测和剩余风险。
- 清单路径：`C:\Users\大小姐\Documents\Codex\2026-06-30\ni\outputs\WordPlusPlus\发布前检查清单.md`
- 状态：已建立，以后每次发布执行。

